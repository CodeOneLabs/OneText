namespace OneText.Unicode
{
    /// <summary>Bidi_Class property values (UAX #9, Table 4).</summary>
    public enum BidiClass : byte
    {
        L, R, AL, EN, ES, ET, AN, CS, NSM, BN, B, S, WS, ON,
        LRE, RLE, LRO, RLO, PDF, LRI, RLI, FSI, PDI,
    }
}
